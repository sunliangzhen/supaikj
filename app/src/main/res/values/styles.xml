<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="bottom_up_down_style">
        <item name="android:windowEnterAnimation">@anim/dialog_enter</item>
        <item name="@android:windowExitAnimation">@anim/dialog_exit</item>
    </style>

    <style name="HeaderStyle">
        <item name="android:background">@color/md_teal_300</item>
        <item name="shv_expandedColor">@color/red</item>
        <item name="shv_collapsedColor">@color/black</item>
    </style>

    <style name="MyTabLayoutTextAppearanceInverse2" parent="TextAppearance.AppCompat.Widget.ActionBar.Title.Inverse">
        <item name="android:textSize">15sp</item>
        <item name="android:textColor">#99ffffff</item>
    </style>

    <style name="AppBaseTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- 状态栏颜色 -->
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <!-- toolbar（actionbar）背景颜色 -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="android:windowBackground">@android:color/white</item>
        <item name="colorAccent">#CF2D28</item>
        <item name="contentInsetStart">0dp</item>
    </style>

    <style name="StyleProgressBarMini" parent="Widget.AppCompat.ProgressBar.Horizontal">

        <item name="android:maxHeight">30dp</item>
        <item name="android:minHeight">10dp</item>
        <item name="android:indeterminateOnly">false</item>
        <item name="android:indeterminateDrawable">
            @android:drawable/progress_indeterminate_horizontal
        </item>
        <item name="android:progressDrawable">@drawable/progress_horizontal</item>

    </style>

    <style name="StyleProgressBarMini2" parent="Widget.AppCompat.ProgressBar.Horizontal">

        <item name="android:maxHeight">30dp</item>
        <item name="android:minHeight">10dp</item>
        <item name="android:indeterminateOnly">false</item>
        <item name="android:indeterminateDrawable">
            @android:drawable/progress_indeterminate_horizontal
        </item>
        <item name="android:progressDrawable">@drawable/progress_horizontal2</item>

    </style>

</resources>
